package Patrones.Singleton; // Permite crear una clase que sólo puede instanciarse una vez, y desde la misma clase

public class Main {
    public static void main(String[] args) {

        Singleton singleton = Singleton.getInstance(); // Instanciamos el singleton mediante una clase estática del mismo

    }
}